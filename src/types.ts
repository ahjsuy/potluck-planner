export interface Dishes {
  id: string;
  dishname: string;
  dishguest: string;
  dishportion: string;
  category: string;
}

export interface Event {
  eventid: string;
  eventname: string;
  address: string;
  date: string;
}
